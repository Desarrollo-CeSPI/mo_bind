# This file was automatically generated by Chef.
# Changes made here will be overwritten.

<% @zones.each do |zone| %>
zone "<%= zone_id(zone) %>" {
  type <%= dns_type_for(zone) %>;
  file "<%= zone_file(zone) %>";
  <% zone_definition(zone, 'options', []).each do |key, values| %>
  <%= sanitize_option(key, values) %>
  <% end %>
};
<% end %>
